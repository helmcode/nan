---
/*
 * /docs/api: la referencia interactiva.
 *
 * Ruta estática, así que gana al catch-all `[...slug].astro` que sirve el resto
 * de la colección de docs. `api.mdx` ya no existe: el contenido sale del spec.
 */
import ApiReference from '../../components/docs/ApiReference.astro';
---

<ApiReference lang="en" />
