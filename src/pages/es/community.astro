---
// Misma página que la versión EN. El idioma lo resuelve getLocale(Astro.url)
// dentro de los componentes, así que no se duplica ni el marcado ni el copy.
import Page from '../community.astro';

export const prerender = false;
---

<Page />
